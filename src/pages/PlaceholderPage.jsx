export default function PlaceholderPage({ title }) {
  return (
    <main className="mx-auto max-w-4xl p-8">
      <h1 className="text-2xl font-semibold">{title}</h1>
      <p className="mt-2 text-gray-600">Placeholder — to be built in a later phase.</p>
    </main>
  )
}
